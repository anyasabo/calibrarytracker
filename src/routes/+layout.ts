// This tells SvelteKit to prerender all pages at build time (static site).
export const prerender = true;

// Use client-side routing (SPA mode) for dynamic routes like /systems/[id].
export const ssr = false;
